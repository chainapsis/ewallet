export function useKeplrSignIn() {
  try {
    console.log(14);

    // setIsSigningIn(true);
    // await eWallet?.signIn("google");
    // const email = await eWallet?.getEmail();
    // const publicKey = await eWallet?.getPublicKey();
    // if (email && publicKey) {
    //   auth.setEmail(email);
    //   auth.setPublicKey(publicKey);
    // }
  } catch (error) {
    console.error(error);
  } finally {
    // setIsSigningIn(false);
  }
}
