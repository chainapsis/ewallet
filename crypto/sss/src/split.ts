export async function split(
  share: string,
  n: number,
  t: number,
): Promise<string[]> {
  return [];
  // const shares = await split(toUint8Array(share), n, t);
  // return shares.map((share) => Buffer.from(share).toString("hex"));
}
